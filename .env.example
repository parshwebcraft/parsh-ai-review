# Supabase (pre-populated — required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# OpenAI (optional — enables real GPT-4o-mini reviews via the ai-review edge function.
# When unset, the app falls back to a built-in heuristic analyzer so reviews still work.)
OPENAI_API_KEY=
